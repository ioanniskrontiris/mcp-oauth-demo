# === Runtime toggles ===
AUTH_MODE=direct           # direct | gateway
TOKEN_MODE=jwt             # jwt | opaque
INTROSPECTION_MODE=direct  # direct | gateway
POLICY_MODE=off            # off | local | remote

# === Service endpoints (current demo) ===
AS_BASE=http://localhost:9092
RS_BASE=http://localhost:9091
CLIENT_REDIRECT_URI=http://localhost:9200/callback
CLIENT_ID=demo-client
